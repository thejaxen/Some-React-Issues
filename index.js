//ReactDOM.render(<p>First project with React and .NET</p>, document.getElementById("root"));

/*ReactDOM.render(
    <div>
        <h1>React Course</h1>
        <p>Building a project with .NET and React</p>
        </div>,
        document.getElementById("root")
    );
*/

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
    <div>
        <h1>Reacte Course</h1>
        <p>Building a project with .NET and React keeps on.</p>
    </div>
);